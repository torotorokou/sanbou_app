# --- 基本方針 ---
# リポジトリ内はLFで保存。各OSでチェックアウトしてもLFに統一。
* text=auto eol=lf

# --- スクリプト/定義ファイル（LF必須：Dockerやシェルが壊れない） ---
*.sh           text eol=lf
Dockerfile     text eol=lf
*.dockerignore text eol=lf
Makefile       text eol=lf
*.conf         text eol=lf
*.service      text eol=lf
*.env.example  text eol=lf
*.yml          text eol=lf
*.yaml         text eol=lf

# --- コード（LFで揃える） ---
*.py   text eol=lf
*.pyi  text eol=lf
*.ts   text eol=lf
*.tsx  text eol=lf
*.js   text eol=lf
*.jsx  text eol=lf
*.json text eol=lf
*.css  text eol=lf
*.html text eol=lf
*.md   text eol=lf

# --- バイナリ（差分無効化） ---
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.pdf  binary
*.woff binary
*.woff2 binary
*.ttf  binary

# --- 生成物や巨大diffのノイズを抑える（任意） ---
dist/**  -diff
build/** -diff
*.min.js -diff

# --- 機密ファイル（Git 追跡を完全に拒否） ---
# これらのファイルは .gitignore でも除外されているが、二重防御として設定
env/.env.*       filter=forbidden
secrets/*.secrets filter=forbidden
secrets/*.json   filter=forbidden
*.pem            filter=forbidden
*.key            filter=forbidden
