# FastAPI >=0.115.7 uses Starlette >=0.49.1 (fixes CVE-2025-62727, CVE-2025-54121)
fastapi>=0.115.7,<0.200.0
uvicorn[standard]==0.33.0
pydantic==2.10.6
pyyaml==6.0.2
python-multipart==0.0.20

# Authentication & Security
# PyJWT with cryptography for ES256 algorithm support (IAP JWT verification)
PyJWT[crypto]==2.10.1
cryptography==44.0.0

# データ加工・解析
pandas==2.2.3

# Excel出力（pandasのto_excel依存）
openpyxl==3.1.5

# PDF出力（必要に応じて。レポート帳票にPDF化）
reportlab==4.2.5

# CSV高速パース（任意）
python-csv==0.0.14

# 日付・時間処理
python-dateutil==2.9.0.post0

# 日本語処理（必要に応じて）
jaconv==0.4.0

# GCS からマスタ/テンプレート同期用
google-cloud-storage==2.19.0

# backend_shared は Dockerfile でインストールされます
