"""
Core layer for ledger_api.

This layer contains:
- domain: Business entities and value objects (no external dependencies)
- ports: Abstract interfaces (Protocols/ABCs) for external interactions
- usecases: Application business logic (depends only on ports)
"""
