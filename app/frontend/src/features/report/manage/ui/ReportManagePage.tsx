import React from "react";
import ReportManagePageLayout from "./ReportManagePageLayout";

/**
 * Report Management Page
 *
 * FSD構造における統合ページコンポーネント
 * 各feature配下のコンポーネントを集約して表示
 */
export const ReportManagePage: React.FC = () => {
  // 実際の実装では、必要なロジックをここに追加
  // 現在はレイアウトコンポーネントをそのまま使用
  return <ReportManagePageLayout />;
};

export default ReportManagePage;
