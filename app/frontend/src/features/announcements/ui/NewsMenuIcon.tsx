/**
 * NewsMenuIcon - お知らせメニューアイコン
 *
 * お知らせのアイコンのみを表示（未読数はラベル側に表示）。
 */

import React from 'react';
import { NotificationOutlined } from '@ant-design/icons';

export const NewsMenuIcon: React.FC = () => {
  return <NotificationOutlined />;
};
