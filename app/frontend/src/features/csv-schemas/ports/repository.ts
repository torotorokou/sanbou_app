/**
 * CSV Tools Feature - Ports Layer
 * (現時点では外部I/Oなし。将来的にファイルアップロードAPIがあれば定義)
 */

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface ICsvRepository {
  // uploadCsv(file: File): Promise<void>;
}
