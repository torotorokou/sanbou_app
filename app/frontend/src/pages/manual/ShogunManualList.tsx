import React from 'react';
import ShogunManualHome from '@/pages/manual/shogunManual';

// 旧「環境将軍マニュアル一覧」の UI をそのまま利用
const ShogunManualList: React.FC = () => {
  return <ShogunManualHome />;
};

export default ShogunManualList;
