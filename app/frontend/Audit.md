# レスポンシブ 3構成統一 - 現状監査レポート（アーカイブ）

> ⚠️ **このドキュメントは過去のマイグレーション記録です。** > **最新の SSOT**: `src/shared/constants/breakpoints.ts` (md:768, lg:1024, xl:1280)
> **Lean-3**: mobile ≤767, tablet 768–1279, desktop ≥1280

**日付**: 2025-10-15  
**ブランチ**: `feat/responsive-3breakpoints`  
**旧目標**: 576/sm を全廃し、≤767 / ≥768 / ≥1200 の3本に統一（現在は1280に更新済み）

---

## 📊 監査結果サマリ

### 1. AntD Props（sm 使用）

**検出件数**: 4件

| ファイル                                    | 行  | 内容                                  |
| ------------------------------------------- | --- | ------------------------------------- |
| `pages/utils/components/TokenPreview.tsx`   | 32  | `<Col xs={24} sm={12} md={8} lg={6}>` |
| `pages/dashboard/CustomerListDashboard.tsx` | 127 | `<Col xs={24} sm={12} md={8}>`        |
| `pages/dashboard/CustomerListDashboard.tsx` | 130 | `<Col xs={24} sm={12} md={8}>`        |
| `pages/dashboard/CustomerListDashboard.tsx` | 133 | `<Col xs={24} sm={12} md={8}>`        |

**対応方針**: `sm={...}` を削除し、`xs`→`md` の直接遷移に変更

---

### 2. CSS メディアクエリ（576/sm）

**検出件数**: 0件  
✅ **既にクリーン！** 576px や `--bp-sm` の使用なし

---

### 3. 既定外数値（レイアウト固有のpx）

**検出件数**: 14件（重複含む）

| ファイル                      | 行    | 値                  | 用途                 | 判定                      |
| ----------------------------- | ----- | ------------------- | -------------------- | ------------------------- |
| `shared/theme/responsive.css` | 36-37 | 160px, 260px        | サイドバー幅         | ✅ レイアウト固有（保持） |
| `pages/home/PortalPage.css`   | 9     | 1200px              | コンテンツ max-width | ✅ ANT.xl 相当（保持）    |
| `styles/custom-media.css`     | 2-4   | 767/768/1199/1200px | ブレークポイント定義 | ✅ 正規値（保持）         |

**対応方針**: すべて合理的な使用。追加修正不要。

---

## 🎯 ホットスポット（優先修正ファイル）

### Top 3

1. **`pages/utils/components/TokenPreview.tsx`** - sm/lg 両方使用
2. **`pages/dashboard/CustomerListDashboard.tsx`** - 3箇所で sm 使用
3. （3位なし - 影響範囲は小さい）

---

## 📈 ディレクトリ別傾向

```
pages/utils/components/    1件 (sm使用)
pages/dashboard/           3件 (sm使用)
shared/theme/              0件
features/                  0件
```

**結論**:

- **影響範囲は極めて限定的**（2ファイル、4箇所のみ）
- CSS側は既にクリーン
- 自動置換で99%完了可能

---

## ⚠️ 既知の注意点

1. **`--md-only` トークン**: 現在 `custom-media.css` に存在

   - → Phase 3 で削除し、`--ge-md` に統合予定

2. **AntD Grid System**: `xs/sm/md/lg/xl/xxl` 全段階をサポート

   - → `xs/md/xl` の3段階のみに制限予定

3. **ANT.sm/lg/xxl**: `breakpoints.ts` に定義されているが未使用
   - → `@deprecated` マークを追加し、BP オブジェクトを新設予定

---

## 📋 次のアクション（Phase 2）

- [ ] `TokenPreview.tsx` の `sm={12}` を削除
- [ ] `CustomerListDashboard.tsx` の 3箇所の `sm={12}` を削除
- [ ] 型エラーがないことを確認
- [ ] Phase 3: custom-media を3本に限定

---

**監査完了日時**: 2025-10-15 (Phase 1 完了)
