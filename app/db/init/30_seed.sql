
-- SET search_path = app, public;



-- /* no-op: intentionally empty to avoid side-effects when run in CI or init steps */
-- -- 30_seed.sql
-- -- 説明: 初期データの投入はこのファイルでは行いません。安全のためコメントのみの no-op スクリプトです。

-- /* no-op: intentionally empty to avoid side-effects when run in CI or init steps */
-- WHERE u.username = 'admin'
